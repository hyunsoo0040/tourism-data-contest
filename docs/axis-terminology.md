# 세 축 명칭 — 2026-09-14

사용자 화면, 공유 이미지, 새 추천 설명, 평가 도구와 보고서의 축 명칭은 다음과 같다.

| 내부 식별자 | 한국어 명칭 |
|---|---|
| H / HISTORY_TRADITION | 대상•원형형 |
| E / EMOTION_IMAGE | 의미•이미지형 |
| R / REST_IMMERSION | 자기•몰입형 |

구분 기호는 사용자 지정 `•`(U+2022)다. 명칭에는 이미 `형`이 포함되어 있다. 세 명칭 모두 받침이 있으므로 연결 조사는 `과`를 쓴다. 이론명, 축의 정의, 문항, 가중치, 점수와 순위 계산은 이번 변경의 대상이 아니다.

현재 `contracts/questionnaire-v2.json`과 Python 생성 정의의 축 명칭·연결 조사를 함께 변경하고 구성 해시를 다시 계산했다. 변경 직전 계약은 `contracts/questionnaire-v2-20260913.json`에 보존했다. 백엔드와 프런트엔드는 이 이전 해시의 저장된 프로필도 그대로 읽는다. 현재와 이전 계약의 문항·점수 행렬·동점 우선순위는 동일하다.

관광지 원문에서 역사와 전통을 일반적으로 설명하는 표현, 출처 원본, 과거 모델 응답·분석·평가·배포 기록, 백업과 원본 UI 참조 자료는 보존한다. 이미 동결된 v1 및 이전 v2 계약, 이전 계약을 재현하는 테스트, Phase 4 융합 프로필의 해시·DB 검증용 직렬화 명칭과 마이그레이션도 과거 자료의 검증을 위해 보존한다. 이 자료를 현재 명칭으로 다시 저장한 것처럼 표시하지 않는다.

현재 화면에 쓰는 축 명칭은 내부 축 ID를 기준으로 선택한다. 현재 목업 추천용 합성 fixture와 새로 생성하는 스토리 PNG에도 위 명칭을 적용한다.
