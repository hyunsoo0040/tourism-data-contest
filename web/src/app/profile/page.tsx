import { SpaHost } from "../spa-host";

/**
 * `/profile` — upstream result/type/character/keyword shell driven by the
 * persisted backend profile through the SPA host.
 */
export default function ProfilePage() {
  return <SpaHost />;
}
