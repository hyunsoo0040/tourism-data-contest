import { SpaHost } from "../spa-host";

/**
 * `/start` — upstream feature chooser (기능.html) exact port plus the
 * upstream-style travel-conditions panel ahead of the quiz. The retained
 * journey shell renders here through the SPA host.
 */
export default function StartPage() {
  return <SpaHost />;
}
