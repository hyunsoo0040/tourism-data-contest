import { SpaHost } from "../../spa-host";

/**
 * `/recommendations/[runId]` — recommendation results through the SPA host
 * using the existing backend adapters and upstream card/panel vocabulary.
 */
export default function RecommendationsPage() {
  return <SpaHost />;
}
