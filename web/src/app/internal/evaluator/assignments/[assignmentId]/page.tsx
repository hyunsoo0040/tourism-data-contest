import { SpaHost } from "../../../../spa-host";

/** `/internal/evaluator/assignments/[assignmentId]` — evaluator workspace. */
export default function InternalEvaluatorAssignmentPage() {
  return <SpaHost />;
}
