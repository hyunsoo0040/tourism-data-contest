import { SpaHost } from "../../../../spa-host";

/** `/internal/operator/assignments/[assignmentId]` — operator adjudication. */
export default function InternalOperatorAssignmentPage() {
  return <SpaHost />;
}
