import { SpaHost } from "../../../../spa-host";

/** `/internal/profile-releases/builder/evidence-review` — evidence review. */
export default function InternalEvidenceReviewPage() {
  return <SpaHost />;
}
