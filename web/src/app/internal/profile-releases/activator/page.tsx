import { SpaHost } from "../../../spa-host";

/** `/internal/profile-releases/activator` — activator console. */
export default function InternalProfileReleaseActivatorPage() {
  return <SpaHost />;
}
