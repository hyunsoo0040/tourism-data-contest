"""Optional photo-preference storage, contract, and provider boundaries.

Submodules are imported directly by consumers so package import remains free
of filesystem, provider, and projection side effects.
"""

__all__: list[str] = []
